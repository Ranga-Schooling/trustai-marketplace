[pytest]
pythonpath = .
markers =
    integration: chained multi-step journeys across auth, listing submission and analysis
    contract: schema/protocol contract checks (AIProvider, AIAnalysisResult, frozen routes) -- no network, mock provider only
